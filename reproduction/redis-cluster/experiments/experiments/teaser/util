#!/usr/bin/env zsh

SCRIPT_DIR=${0:a:h}

for data in 0 25 50 75 100 125 150 175 200; do

    ${SCRIPT_DIR}/../../patch-benchmark/scripts/notify "Start data: $data"
    execute $data
    ${SCRIPT_DIR}/../../patch-benchmark/scripts/notify "End data: $data"
done

${SCRIPT_DIR}/../../collect-results ${experiment_name}

